# Copy to .env and fill in values

# Backend API base URL (used in production).
# In development the Vite proxy rewrites /api/* -> http://localhost:8080/*
# so you do NOT need to set this locally.
VITE_API_BASE=http://172.16.4.104:8080
